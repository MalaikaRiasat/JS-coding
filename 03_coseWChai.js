const user_id = 1212; //canot change value in futer
let name="MALAIKA";//change value any time
fatherName="Riasat";// js provide the facelity that we assign variable without any data type.==> but the is not good practics

console.log(name); //single output 
console.table([user_id,name,fatherName]) // all variable show in form of table at same time